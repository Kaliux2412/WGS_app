import 'package:flutter/material.dart';

class ConectarPage extends StatelessWidget {
  const ConectarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Conectar'),);
  }
}