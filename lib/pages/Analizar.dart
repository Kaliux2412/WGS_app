import 'package:flutter/material.dart';

class AnalizarPage extends StatefulWidget {
  const AnalizarPage({super.key});

  @override
  State<AnalizarPage> createState() => _AnalizarState();
}

class _AnalizarState extends State<AnalizarPage> {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Analizar'),);
  }
}